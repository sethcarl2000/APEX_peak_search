#include <Fcn1D/Gauss.hpp>
#include <numbers.hpp> 

//stdlib 
#include <cmath> 


namespace {
    const double sqrt_twopi = std::sqrt(2.*peak_search::numbers::pi); 
};

namespace peak_search
{

//_______________________________________________________________________________________________________________________________
Gauss::Gauss(double mu, double _x0, double _sigma)
    : Fcn1D({mu}), sigma{_sigma}, x0{_x0}
{}
//_______________________________________________________________________________________________________________________________
double Gauss::operator()(double x) const 
{
    double mu = par[0]; 

    double arg = (x - x0)/sigma; 

    return mu * std::exp( -0.5*arg*arg )/(sigma * sqrt_twopi); 
}
//_______________________________________________________________________________________________________________________________
double Gauss::Di(double x, int i) const
{
    if (i != 0) return 0.; 

    double arg = (x - x0)/sigma; 

    return std::exp( -0.5*arg*arg )/(sigma * sqrt_twopi);
}
//_______________________________________________________________________________________________________________________________
//_______________________________________________________________________________________________________________________________
double Gauss::Di_Dj(double x, int i, int j) const
{
    return 0.; 
}
//_______________________________________________________________________________________________________________________________
//_______________________________________________________________________________________________________________________________
//_______________________________________________________________________________________________________________________________


}; 